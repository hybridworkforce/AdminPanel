

function init_chart_doughnut() {
    if ("undefined" != typeof Chart && (console.log("init_chart_doughnut"), $(".canvasDoughnut").length)) {
        var a = {
            type: "doughnut",
            tooltipFillColor: "rgba(51, 51, 51, 0.55)",
            data: {
                labels: [ "Rules", "Invoices",],
                datasets: [ {
                    data: [ 11,5 ],
                    backgroundColor: [ "#006666", "#9B59B6" ],
                    hoverBackgroundColor: [ "#006666", "#B370CF"  ]
                } ]
            },
            options: {
                legend: !1,
                responsive: !1
            }
        };
        $(".canvasDoughnut").each(function() {
            var b = $(this);
            new Chart(b, a);
        });
    }
}


$.fn.popover.Constructor.prototype.leave = function(a) {
    var b, c, d = a instanceof this.constructor ? a : $(a.currentTarget)[this.type](this.getDelegateOptions()).data("bs." + this.type);
    originalLeave.call(this, a), a.currentTarget && (b = $(a.currentTarget).siblings(".popover"), 
    c = d.timeout, b.one("mouseenter", function() {
        clearTimeout(c), b.one("mouseleave", function() {
            $.fn.popover.Constructor.prototype.leave.call(d, d);
        });
    }));
}, $("body").popover({
    selector: "[data-popover]",
    trigger: "click hover",
    delay: {
        show: 50,
        hide: 400
    }
}), $(document).ready(function() {
    
	
	
	 
	init_chart_doughnut();
});